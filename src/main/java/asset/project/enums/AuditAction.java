package asset.project.enums;

public enum AuditAction {
    asset_created,
    asset_updated,
    asset_archived,
    asset_deleted,
    asset_assigned,
    asset_transferred,
    validation_initiated,
    validation_record_updated,
    role_assigned,
    role_revoked,
    user_created,
    user_deactivated
}